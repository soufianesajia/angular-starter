export class CalendarChartConfig {

  title: string;
  width: number;
  height: number;

  constructor(title: string, width: number, height: number) {
    this.title = title;
    this.width = width;
    this.height = height;
  }
}
